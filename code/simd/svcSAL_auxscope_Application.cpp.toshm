		for (unsigned int count=0; count<5; count++) {
			auxscope_Application_ref->Demand[count] = samples[i].Demand[count];
		}
		for (unsigned int count=0; count<5; count++) {
			auxscope_Application_ref->Error[count] = samples[i].Error[count];
		}
		for (unsigned int count=0; count<5; count++) {
			auxscope_Application_ref->Position[count] = samples[i].Position[count];
		}
		auxscope_Application_ref->syncI++;
