		for (unsigned int count=0; count<17; count++) {
			auxscope_Camera_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			auxscope_Camera_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		auxscope_Camera_ref->syncI++;
