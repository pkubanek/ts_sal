		for (unsigned int count=0; count<17; count++) {
			d.Raw[count] = auxscope_Spectrometer_ref->Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			d.Calibrated[count] = auxscope_Spectrometer_ref->Calibrated[count];
		}
		auxscope_Spectrometer_ref->syncO = 1;
