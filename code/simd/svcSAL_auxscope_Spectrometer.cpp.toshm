		for (unsigned int count=0; count<17; count++) {
			auxscope_Spectrometer_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			auxscope_Spectrometer_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		auxscope_Spectrometer_ref->syncI++;
