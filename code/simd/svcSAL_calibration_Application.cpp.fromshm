		strncpy(d.Status,calibration_Application_ref->Status,16);
		for (unsigned int count=0; count<5; count++) {
			d.Demand[count] = calibration_Application_ref->Demand[count];
		}
		for (unsigned int count=0; count<5; count++) {
			d.Error[count] = calibration_Application_ref->Error[count];
		}
		for (unsigned int count=0; count<5; count++) {
			d.Position[count] = calibration_Application_ref->Position[count];
		}
		calibration_Application_ref->syncO = 1;
