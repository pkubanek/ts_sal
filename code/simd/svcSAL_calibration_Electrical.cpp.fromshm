		for (unsigned int count=0; count<17; count++) {
			d.Raw[count] = calibration_Electrical_ref->Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			d.Calibrated[count] = calibration_Electrical_ref->Calibrated[count];
		}
		calibration_Electrical_ref->syncO = 1;
