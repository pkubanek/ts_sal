		calibration_response_ref->repeat = samples[i].repeat;
		calibration_response_ref->submits = samples[i].submits;
		calibration_response_ref->cmdID = samples[i].cmdID;
		calibration_response_ref->timeout = samples[i].timeout;
		strncpy(calibration_response_ref->ack,samples[i].ack,16);
		strncpy(calibration_response_ref->error,samples[i].error,16);
		strncpy(calibration_response_ref->result,samples[i].result,64);
		calibration_response_ref->syncI++;
