		for (unsigned int count=0; count<202; count++) {
			camera_BEE_biases_ref->operate[count] = samples[i].operate[count];
		}
		for (unsigned int count=0; count<202; count++) {
			camera_BEE_biases_ref->reset[count] = samples[i].reset[count];
		}
		for (unsigned int count=0; count<202; count++) {
			camera_BEE_biases_ref->substrate[count] = samples[i].substrate[count];
		}
		camera_BEE_biases_ref->syncI++;
