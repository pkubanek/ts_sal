		d.raftID = camera_BEE_thermal_ref->raftID;
		d.cfgChkSum = camera_BEE_thermal_ref->cfgChkSum;
		for (unsigned int count=0; count<11; count++) {
			d.heaterStatus[count] = camera_BEE_thermal_ref->heaterStatus[count];
		}
		for (unsigned int count=0; count<11; count++) {
			d.heaterPower[count] = camera_BEE_thermal_ref->heaterPower[count];
		}
		for (unsigned int count=0; count<21; count++) {
			d.temp[count] = camera_BEE_thermal_ref->temp[count];
		}
		camera_BEE_thermal_ref->syncO = 1;
