		for (unsigned int count=0; count<26; count++) {
			camera_PWR_ref->current[count] = samples[i].current[count];
		}
		for (unsigned int count=0; count<26; count++) {
			camera_PWR_ref->supply_voltage[count] = samples[i].supply_voltage[count];
		}
		camera_PWR_ref->syncI++;
