		for (unsigned int count=0; count<21; count++) {
			camera_SDS_status_ref->flags[count] = samples[i].flags[count];
		}
		for (unsigned int count=0; count<81; count++) {
			camera_SDS_status_ref->current[count] = samples[i].current[count];
		}
		camera_SDS_status_ref->syncI++;
