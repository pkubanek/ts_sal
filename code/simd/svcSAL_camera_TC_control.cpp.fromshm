		for (unsigned int count=0; count<11; count++) {
			d.status[count] = camera_TC_control_ref->status[count];
		}
		for (unsigned int count=0; count<11; count++) {
			d.setpoint[count] = camera_TC_control_ref->setpoint[count];
		}
		for (unsigned int count=0; count<11; count++) {
			d.temperature[count] = camera_TC_control_ref->temperature[count];
		}
		camera_TC_control_ref->syncO = 1;
