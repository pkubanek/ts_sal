		for (unsigned int count=0; count<11; count++) {
			camera_TC_control_ref->status[count] = samples[i].status[count];
		}
		for (unsigned int count=0; count<11; count++) {
			camera_TC_control_ref->setpoint[count] = samples[i].setpoint[count];
		}
		for (unsigned int count=0; count<11; count++) {
			camera_TC_control_ref->temperature[count] = samples[i].temperature[count];
		}
		camera_TC_control_ref->syncI++;
