		for (unsigned int count=0; count<51; count++) {
			camera_TC_zone3_ref->status[count] = samples[i].status[count];
		}
		for (unsigned int count=0; count<51; count++) {
			camera_TC_zone3_ref->setpoint[count] = samples[i].setpoint[count];
		}
		for (unsigned int count=0; count<51; count++) {
			camera_TC_zone3_ref->temperature[count] = samples[i].temperature[count];
		}
		camera_TC_zone3_ref->syncI++;
