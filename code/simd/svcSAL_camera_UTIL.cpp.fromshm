		for (unsigned int count=0; count<11; count++) {
			d.status[count] = camera_UTIL_ref->status[count];
		}
		for (unsigned int count=0; count<11; count++) {
			d.flow_rate[count] = camera_UTIL_ref->flow_rate[count];
		}
		for (unsigned int count=0; count<11; count++) {
			d.setpoint[count] = camera_UTIL_ref->setpoint[count];
		}
		for (unsigned int count=0; count<11; count++) {
			d.temperature[count] = camera_UTIL_ref->temperature[count];
		}
		camera_UTIL_ref->syncO = 1;
