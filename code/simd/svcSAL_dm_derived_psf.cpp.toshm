		for (unsigned int count=0; count<201; count++) {
			dm_derived_psf_ref->metric1[count] = samples[i].metric1[count];
		}
		for (unsigned int count=0; count<201; count++) {
			dm_derived_psf_ref->metric2[count] = samples[i].metric2[count];
		}
		for (unsigned int count=0; count<201; count++) {
			dm_derived_psf_ref->metric3[count] = samples[i].metric3[count];
		}
		for (unsigned int count=0; count<201; count++) {
			dm_derived_psf_ref->metric4[count] = samples[i].metric4[count];
		}
		for (unsigned int count=0; count<201; count++) {
			dm_derived_psf_ref->metric5[count] = samples[i].metric5[count];
		}
		for (unsigned int count=0; count<201; count++) {
			dm_derived_psf_ref->metric6[count] = samples[i].metric6[count];
		}
		dm_derived_psf_ref->syncI++;
