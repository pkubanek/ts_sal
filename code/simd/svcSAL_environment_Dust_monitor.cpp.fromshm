		for (unsigned int count=0; count<17; count++) {
			d.Raw[count] = environment_Dust_monitor_ref->Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			d.Calibrated[count] = environment_Dust_monitor_ref->Calibrated[count];
		}
		environment_Dust_monitor_ref->syncO = 1;
