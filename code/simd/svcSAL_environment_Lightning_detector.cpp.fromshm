		for (unsigned int count=0; count<17; count++) {
			d.Raw[count] = environment_Lightning_detector_ref->Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			d.Calibrated[count] = environment_Lightning_detector_ref->Calibrated[count];
		}
		environment_Lightning_detector_ref->syncO = 1;
