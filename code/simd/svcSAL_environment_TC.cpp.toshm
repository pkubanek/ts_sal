		for (unsigned int count=0; count<65; count++) {
			environment_TC_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<65; count++) {
			environment_TC_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		environment_TC_ref->syncI++;
