		for (unsigned int count=0; count<17; count++) {
			d.Raw[count] = environment_Video_cameras_ref->Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			d.Calibrated[count] = environment_Video_cameras_ref->Calibrated[count];
		}
		environment_Video_cameras_ref->syncO = 1;
