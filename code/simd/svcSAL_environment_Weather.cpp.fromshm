		for (unsigned int count=0; count<129; count++) {
			d.Raw[count] = environment_Weather_ref->Raw[count];
		}
		for (unsigned int count=0; count<257; count++) {
			d.Calibrated[count] = environment_Weather_ref->Calibrated[count];
		}
		environment_Weather_ref->syncO = 1;
