		for (unsigned int count=0; count<7; count++) {
			hexapod_Application_ref->Demand[count] = samples[i].Demand[count];
		}
		for (unsigned int count=0; count<7; count++) {
			hexapod_Application_ref->Error[count] = samples[i].Error[count];
		}
		for (unsigned int count=0; count<7; count++) {
			hexapod_Application_ref->Position[count] = samples[i].Position[count];
		}
		hexapod_Application_ref->syncI++;
