		for (unsigned int count=0; count<17; count++) {
			hexapod_TC_ref->status[count] = samples[i].status[count];
		}
		for (unsigned int count=0; count<17; count++) {
			hexapod_TC_ref->error[count] = samples[i].error[count];
		}
		for (unsigned int count=0; count<17; count++) {
			hexapod_TC_ref->setpoint[count] = samples[i].setpoint[count];
		}
		for (unsigned int count=0; count<17; count++) {
			hexapod_TC_ref->temperature[count] = samples[i].temperature[count];
		}
		hexapod_TC_ref->syncI++;
