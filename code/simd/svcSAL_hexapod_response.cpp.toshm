		hexapod_response_ref->repeat = samples[i].repeat;
		hexapod_response_ref->submits = samples[i].submits;
		hexapod_response_ref->cmdID = samples[i].cmdID;
		hexapod_response_ref->timeout = samples[i].timeout;
		strncpy(hexapod_response_ref->ack,samples[i].ack,16);
		strncpy(hexapod_response_ref->error,samples[i].error,16);
		strncpy(hexapod_response_ref->result,samples[i].result,64);
		hexapod_response_ref->syncI++;
