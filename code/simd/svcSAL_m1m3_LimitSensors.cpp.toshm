		for (unsigned int count=0; count<65; count++) {
			m1m3_LimitSensors_ref->liftoff[count] = samples[i].liftoff[count];
		}
		for (unsigned int count=0; count<65; count++) {
			m1m3_LimitSensors_ref->limit[count] = samples[i].limit[count];
		}
		m1m3_LimitSensors_ref->syncI++;
