		for (unsigned int count=0; count<17; count++) {
			m1m3_Support_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			m1m3_Support_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		m1m3_Support_ref->syncI++;
