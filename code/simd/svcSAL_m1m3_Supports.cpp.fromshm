		for (unsigned int count=0; count<7; count++) {
			d.stepcnt[count] = m1m3_Supports_ref->stepcnt[count];
		}
		for (unsigned int count=0; count<7; count++) {
			d.targetpos[count] = m1m3_Supports_ref->targetpos[count];
		}
		for (unsigned int count=0; count<7; count++) {
			d.force[count] = m1m3_Supports_ref->force[count];
		}
		m1m3_Supports_ref->syncO = 1;
