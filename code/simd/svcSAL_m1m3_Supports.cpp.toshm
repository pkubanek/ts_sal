		for (unsigned int count=0; count<7; count++) {
			m1m3_Supports_ref->stepcnt[count] = samples[i].stepcnt[count];
		}
		for (unsigned int count=0; count<7; count++) {
			m1m3_Supports_ref->targetpos[count] = samples[i].targetpos[count];
		}
		for (unsigned int count=0; count<7; count++) {
			m1m3_Supports_ref->force[count] = samples[i].force[count];
		}
		m1m3_Supports_ref->syncI++;
