		for (unsigned int count=0; count<16; count++) {
			d.Status[count] = m2_Actuators_ref->Status[count];
		}
		for (unsigned int count=0; count<16; count++) {
			d.Raw[count] = m2_Actuators_ref->Raw[count];
		}
		for (unsigned int count=0; count<32; count++) {
			d.Calibrated[count] = m2_Actuators_ref->Calibrated[count];
		}
		m2_Actuators_ref->syncO = 1;
