		for (unsigned int count=0; count<32; count++) {
			m2_Hexapod_ref->Status[count] = samples[i].Status[count];
		}
		for (unsigned int count=0; count<32; count++) {
			m2_Hexapod_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<32; count++) {
			m2_Hexapod_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		m2_Hexapod_ref->syncI++;
