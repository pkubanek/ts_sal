		for (unsigned int count=0; count<17; count++) {
			m2_Metrology_ref->status[count] = samples[i].status[count];
		}
		for (unsigned int count=0; count<17; count++) {
			m2_Metrology_ref->distance[count] = samples[i].distance[count];
		}
		for (unsigned int count=0; count<17; count++) {
			m2_Metrology_ref->error[count] = samples[i].error[count];
		}
		m2_Metrology_ref->syncI++;
