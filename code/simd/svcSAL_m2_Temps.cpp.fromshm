		for (unsigned int count=0; count<16; count++) {
			d.Health[count] = m2_Temps_ref->Health[count];
		}
		for (unsigned int count=0; count<16; count++) {
			d.Raw_[count] = m2_Temps_ref->Raw_[count];
		}
		for (unsigned int count=0; count<16; count++) {
			d.Calibrated[count] = m2_Temps_ref->Calibrated[count];
		}
		m2_Temps_ref->syncO = 1;
