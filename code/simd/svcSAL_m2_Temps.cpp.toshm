		for (unsigned int count=0; count<16; count++) {
			m2_Temps_ref->Health[count] = samples[i].Health[count];
		}
		for (unsigned int count=0; count<16; count++) {
			m2_Temps_ref->Raw_[count] = samples[i].Raw_[count];
		}
		for (unsigned int count=0; count<16; count++) {
			m2_Temps_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		m2_Temps_ref->syncI++;
