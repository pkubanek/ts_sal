		for (unsigned int count=0; count<73; count++) {
			m2_actuators_ref->stepcnt[count] = samples[i].stepcnt[count];
		}
		for (unsigned int count=0; count<73; count++) {
			m2_actuators_ref->targetpos[count] = samples[i].targetpos[count];
		}
		for (unsigned int count=0; count<73; count++) {
			m2_actuators_ref->force[count] = samples[i].force[count];
		}
		m2_actuators_ref->syncI++;
