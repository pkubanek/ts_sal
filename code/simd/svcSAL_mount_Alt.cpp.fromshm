		for (unsigned int count=0; count<17; count++) {
			d.Raw[count] = mount_Alt_ref->Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			d.Calibrated[count] = mount_Alt_ref->Calibrated[count];
		}
		mount_Alt_ref->syncO = 1;
