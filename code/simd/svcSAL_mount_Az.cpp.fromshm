		for (unsigned int count=0; count<17; count++) {
			d.Raw[count] = mount_Az_ref->Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			d.Calibrated[count] = mount_Az_ref->Calibrated[count];
		}
		mount_Az_ref->syncO = 1;
