		network_response_ref->repeat = samples[i].repeat;
		network_response_ref->submits = samples[i].submits;
		network_response_ref->cmdID = samples[i].cmdID;
		network_response_ref->timeout = samples[i].timeout;
		strncpy(network_response_ref->ack,samples[i].ack,16);
		strncpy(network_response_ref->error,samples[i].error,16);
		strncpy(network_response_ref->result,samples[i].result,64);
		network_response_ref->syncI++;
