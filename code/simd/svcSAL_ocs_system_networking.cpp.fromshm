		strncpy(d.mac,ocs_system_networking_ref->mac,32);
		strncpy(d.subsystem,ocs_system_networking_ref->subsystem,128);
		for (unsigned int count=0; count<5; count++) {
			d.ip[count] = ocs_system_networking_ref->ip[count];
		}
		for (unsigned int count=0; count<7; count++) {
			d.rx[count] = ocs_system_networking_ref->rx[count];
		}
		for (unsigned int count=0; count<7; count++) {
			d.tx[count] = ocs_system_networking_ref->tx[count];
		}
		ocs_system_networking_ref->syncO = 1;
