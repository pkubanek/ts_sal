		for (unsigned int count=0; count<17; count++) {
			power_Electrical_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			power_Electrical_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		power_Electrical_ref->syncI++;
