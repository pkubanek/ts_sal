		power_response_ref->repeat = samples[i].repeat;
		power_response_ref->submits = samples[i].submits;
		power_response_ref->cmdID = samples[i].cmdID;
		power_response_ref->timeout = samples[i].timeout;
		strncpy(power_response_ref->ack,samples[i].ack,16);
		strncpy(power_response_ref->error,samples[i].error,16);
		strncpy(power_response_ref->result,samples[i].result,64);
		power_response_ref->syncI++;
