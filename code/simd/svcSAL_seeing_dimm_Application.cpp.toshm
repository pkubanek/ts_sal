		strncpy(seeing_dimm_Application_ref->Status,samples[i].Status,16);
		for (unsigned int count=0; count<17; count++) {
			seeing_dimm_Application_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			seeing_dimm_Application_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		for (unsigned int count=0; count<5; count++) {
			seeing_dimm_Application_ref->Error[count] = samples[i].Error[count];
		}
		seeing_dimm_Application_ref->syncI++;
