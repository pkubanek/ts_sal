		for (unsigned int count=0; count<16; count++) {
			d.Status[count] = seeing_dimm_Electrical_ref->Status[count];
		}
		for (unsigned int count=0; count<17; count++) {
			d.Raw[count] = seeing_dimm_Electrical_ref->Raw[count];
		}
		for (unsigned int count=0; count<32; count++) {
			d.Calibrated[count] = seeing_dimm_Electrical_ref->Calibrated[count];
		}
		seeing_dimm_Electrical_ref->syncO = 1;
