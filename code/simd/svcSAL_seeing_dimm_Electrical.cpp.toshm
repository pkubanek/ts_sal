		for (unsigned int count=0; count<16; count++) {
			seeing_dimm_Electrical_ref->Status[count] = samples[i].Status[count];
		}
		for (unsigned int count=0; count<17; count++) {
			seeing_dimm_Electrical_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<32; count++) {
			seeing_dimm_Electrical_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		seeing_dimm_Electrical_ref->syncI++;
