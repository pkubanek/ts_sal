		for (unsigned int count=0; count<16; count++) {
			seeing_dimm_Temps_ref->Health[count] = samples[i].Health[count];
		}
		for (unsigned int count=0; count<16; count++) {
			seeing_dimm_Temps_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<16; count++) {
			seeing_dimm_Temps_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		seeing_dimm_Temps_ref->syncI++;
