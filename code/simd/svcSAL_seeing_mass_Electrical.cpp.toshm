		for (unsigned int count=0; count<17; count++) {
			seeing_mass_Electrical_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<33; count++) {
			seeing_mass_Electrical_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		seeing_mass_Electrical_ref->syncI++;
