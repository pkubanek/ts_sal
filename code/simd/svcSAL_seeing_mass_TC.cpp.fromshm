		for (unsigned int count=0; count<17; count++) {
			d.Raw[count] = seeing_mass_TC_ref->Raw[count];
		}
		for (unsigned int count=0; count<17; count++) {
			d.Calibrated[count] = seeing_mass_TC_ref->Calibrated[count];
		}
		seeing_mass_TC_ref->syncO = 1;
