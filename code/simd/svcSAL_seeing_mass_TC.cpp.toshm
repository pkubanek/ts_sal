		for (unsigned int count=0; count<17; count++) {
			seeing_mass_TC_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<17; count++) {
			seeing_mass_TC_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		seeing_mass_TC_ref->syncI++;
