		for (unsigned int count=0; count<17; count++) {
			d.Raw[count] = skycam_Metrology_ref->Raw[count];
		}
		for (unsigned int count=0; count<17; count++) {
			d.Calibrated[count] = skycam_Metrology_ref->Calibrated[count];
		}
		skycam_Metrology_ref->syncO = 1;
