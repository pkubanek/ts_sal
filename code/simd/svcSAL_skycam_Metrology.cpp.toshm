		for (unsigned int count=0; count<17; count++) {
			skycam_Metrology_ref->Raw[count] = samples[i].Raw[count];
		}
		for (unsigned int count=0; count<17; count++) {
			skycam_Metrology_ref->Calibrated[count] = samples[i].Calibrated[count];
		}
		skycam_Metrology_ref->syncI++;
