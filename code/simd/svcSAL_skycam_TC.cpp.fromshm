		for (unsigned int count=0; count<17; count++) {
			d.Raw[count] = skycam_TC_ref->Raw[count];
		}
		for (unsigned int count=0; count<17; count++) {
			d.Calibrated[count] = skycam_TC_ref->Calibrated[count];
		}
		skycam_TC_ref->syncO = 1;
