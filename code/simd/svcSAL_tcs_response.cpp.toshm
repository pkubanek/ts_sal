		tcs_response_ref->repeat = samples[i].repeat;
		tcs_response_ref->submits = samples[i].submits;
		tcs_response_ref->cmdID = samples[i].cmdID;
		tcs_response_ref->timeout = samples[i].timeout;
		strncpy(tcs_response_ref->ack,samples[i].ack,16);
		strncpy(tcs_response_ref->error,samples[i].error,16);
		strncpy(tcs_response_ref->result,samples[i].result,64);
		tcs_response_ref->syncI++;
